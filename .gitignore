# 1. 가상환경 (용량이 크고 로컬 종속적이므로 제외)
.venv/
venv/
env/

# 2. 보안 및 설정 파일 (API 키, DB 비밀번호 등 유출 방지)
default_info/
.env
api.txt
db_config.txt

# 3. 데이터 파일 (용량이 큰 원본 데이터 제외)
raw_data/
__pycache__/
*.pyc
*.DS_Store

# 4. IDE 설정 파일 (불필요한 설정 공유 방지)
.idea/
.vscode/